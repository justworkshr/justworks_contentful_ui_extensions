import { Component, CSSProperties } from 'react';
import { iconName } from './constants';
export declare type IconType = keyof typeof iconName;
export declare type IconColorType = 'primary' | 'positive' | 'negative' | 'warning' | 'secondary' | 'muted' | 'white';
export declare type IconSize = 'tiny' | 'small' | 'large';
export interface IconProps {
    size?: IconSize;
    color?: IconColorType;
    style?: CSSProperties;
    icon: IconType;
    className?: string;
    testId?: string;
}
declare const defaultProps: {
    testId: string;
    size: string;
    color: string;
};
export declare class Icon extends Component<IconProps & typeof defaultProps> {
    static defaultProps: {
        testId: string;
        size: string;
        color: string;
    };
    render(): JSX.Element;
}
export default Icon;
