import { CSSProperties } from 'react';
import { navigationIconName } from './constants';
export declare type NavigationIconType = keyof typeof navigationIconName;
export declare type NavigationIconColorType = 'primary' | 'positive' | 'negative' | 'warning' | 'secondary' | 'muted' | 'white';
export declare type NavigationIconSize = 'small' | 'medium' | 'large' | 'xlarge';
export declare type NavigationIconTagType = 'div' | 'span';
export interface NavigationIconProps {
    size?: NavigationIconSize;
    color?: NavigationIconColorType;
    style?: CSSProperties;
    icon: NavigationIconType;
    className?: string;
    testId?: string;
    tag: NavigationIconTagType;
}
export declare const NavigationIcon: {
    ({ className, icon, size, color, testId, tag, ...otherProps }: NavigationIconProps): JSX.Element;
    defaultProps: {
        testId: string;
        size: string;
        color: string;
        tag: string;
    };
};
export default NavigationIcon;
