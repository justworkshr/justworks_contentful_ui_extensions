import { Component } from 'react';
import { IllustrationProps } from '../../Illustration';
import { AssetType } from '../Asset';
export declare type AssetIconProps = {
    type?: AssetType;
} & Omit<IllustrationProps, 'illustration'> & typeof defaultProps;
declare const defaultProps: {
    type: string;
    testId: string;
};
/**
 * Renders only the Illustration that would represent this asset's type
 */
export declare class AssetIcon extends Component<AssetIconProps> {
    static defaultProps: {
        type: string;
        testId: string;
    };
    render(): JSX.Element;
}
export default AssetIcon;
